
PGENGINE=${VIP_ROAM_DIR}
PGPORT=${VIP_POSTGRESQL_PORT}
PGDATA=${VIP_POSTGRESQL_DATABASE}
PGLOG=${VIP_POSTGRESQL_LOG}
# Log file for pg_upgrade
PGUPLOG=${VIP_POSTGRESQL_LOG_DIR}/upgrade.log

pidfile=${VIP_POSTGRESQL_PID_DIR}/postgresql.pid
lockfile=${VIP_POSTGRESQL_LOCK_DIR}/postgresql

PG_USER=${VIP_POSTGRESQL_USER}
PG_GROUP=${VIP_DAEMON_GROUP}

PGOPTS=

#export POSTGIS_ENABLE_OUTDB_RASTERS=1
#export POSTGIS_GDAL_ENABLED_DRIVERS=ENABLE_ALL
#export POSTGIS_GDAL_ENABLED_DRIVERS="GTiff PNG JPEG GIF XYZ"

PG_START_TIMEOUT=10

PG_AUTH=${NPG_POSTGRESQL_AUTH}

if [ $(id -u) -ne 0 ]; then
  PG_USER=`id -u -n`
  PG_GROUP=`id -g -n`
fi

umask 0002
