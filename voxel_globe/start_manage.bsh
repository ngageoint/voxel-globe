#!/bin/bash

os=`uname`

if [ "${os}" == "Linux" ] || [ "${os}" == "Darwin" ]; then
  $(dirname ${BASH_SOURCE[0]})/../wrap python $(dirname ${BASH_SOURCE[0]})/manage.py "${@}"
else
  $(dirname ${BASH_SOURCE[0]})/../wrap python.exe $(dirname ${BASH_SOURCE[0]})/manage.py "${@}"
fi
